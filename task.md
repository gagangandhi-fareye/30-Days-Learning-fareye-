# Here I will be uploading the details 
# Day 1 : History of Linux , file system heirarchy , Basic commands , creating file or directory, user management , Group Management 
- History of Linux : Linux Came from a unix Family , linux is a free open source software operating system which linus torvalds developed in september 1991.

----- file system heirarchy -------
- File System Heirarchy : In linux everthing is represented as a file including a hardware program , the files are stored in a directory , and every directory contains a filw with a tree structure. linux uses single rooted tree-like structure.
root represnt with /(forward slash ) it is a top level directory in linux.
/ base = this is the starting point of fsh. every directory arises from the root directory. represented by a forward slash (/).
/root = it is home directory for the root user.
/bin = contains binary executables
common linux command yu need to use in single user modes are located under this directory. 
/sbin = system binaries = contains binary executables. but linux command under this dirsctory are used by system administrator, for system maintenance purpsoe.
/dev = it conatins hardware device files . contains devoice files , these includes terminal devices, usb , or any any device attached to the system. 
/var = variable data files such as log files located in /var directory. 
/var/log = system log files generated by os and other applications 
/var/lib conatins database and package files.
/var/mail contains emails 
/var/tmp: conatins temporary files needed for reboot.
/mnt = mount directory = this directory is used to mount a file system temporaray.
this directory is used to mount a fst.
/usr = user binaries the /usr directory contains application sand file used by users , as ooposed to applications and file used by the system.
/etc = configuration files : it contains all the configuration files of the server
/home = secondary user home directory.
/tmp = temporary files 

--- Basics commands ----
pwd = current working dir.
ls = list files.
uname = it shows the name of the kernel(os)
uname -r = it shows version of the kernel 
cd = chnage directory 
clear = it is use to clear screen 
whoami = shows current login user name 
history = it shows list of previously used commands 
date = it shows time and date 

---creating files and directory ---
mkdir dir_name = creating single directory 
mkdir dev qa test = creating multiple dir 
mkdir -p /dev/qa/test/devops = creating directory path 
touch file_name = creating a file in linux 
touch file_names with spaces= creating multiple files 
---- for copy and paste -----
cp = command is used for copy and paste 
cp <options> <source> <destination>
cp -rvf /root/anaconda.cfg /home 
----- user management -----
